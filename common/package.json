{
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "@azure/cosmos": "^3.16.1",
    "@azure/storage-blob": "^12.10.0"
  }
}
