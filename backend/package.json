{
  "name": "@project-carbon/backend",
  "version": "1.0.0",
  "description": "Project Carbon Backend",
  "author": "TeamsCode",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.23.5",
    "fastify": "^3.29.0"
  }
}